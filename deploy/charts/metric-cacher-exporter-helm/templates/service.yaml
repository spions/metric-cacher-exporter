apiVersion: v1
kind: Service
metadata:
  name: {{ include "metric-cacher-exporter.fullname" . }}
  labels:
    {{- include "metric-cacher-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metric-cacher-exporter.selectorLabels" . | nindent 4 }}
